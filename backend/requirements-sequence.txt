-r requirements.txt
# Kaptive assembly typing requires minimap2 to be installed separately on the host.
# Species confirmation requires fastANI; both binaries are reported by the capability endpoint.
kaptive==3.2.0
# Install requirements-embedding.txt in the dedicated feature-extraction environment.
